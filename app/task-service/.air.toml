# air.toml

[build]
cmd = "go build -o tmp/task-service ./cmd"
bin = "tmp/task-service"
watch_dir = "."
include_ext = ["go", "tpl", "tmpl", "html"]
exclude_dir = ["tmp", "vendor"]

[log]
time = true

