version = "test"
# air.toml

[build]
cmd = "go build -o tmp/company-service ./cmd"
bin = "tmp/company-service"
watch_dir = "."
include_ext = ["go", "tpl", "tmpl", "html"]
exclude_dir = ["tmp", "vendor"]

[log]
time = true
